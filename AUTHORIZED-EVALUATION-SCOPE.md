# 授权评测与隔离边界

## 1. 项目性质

本仓库用于研究能够调用工具并改变环境状态的 Agent 在受控办公场景中的行为。
所有评测均在项目维护者控制的 Docker 隔离环境内运行，目标是验证权限、授权、策略、
状态一致性、审计证据、可重放性和失败关闭机制。

本项目不是生产办公系统，不连接或代表真实的 Microsoft 365、邮件、银行、支付或其他
第三方服务。

## 2. 授权范围

- 只测试项目维护者拥有并控制的 Docker 内确定性 Office 模拟系统。
- 所有人员、组织、账户、邮件、日历、文件、资源标识和业务状态均为合成数据。
- 模型只能操作一次性容器中的模拟工具和模拟状态。
- 测试目标仅限于观察和验证 Agent 的工具调用、权限分支、状态转换和审计结果。
- 可以构造正常任务、受控异常输入、授权变化和策略拒绝，用于验证系统是否按合同运行。
- 可以记录、重放和比较模拟环境中的轨迹，但不得把测试步骤迁移到真实系统。

## 3. 明确禁止

- 不连接真实邮箱、银行、Microsoft 365、生产账户或其他第三方系统。
- 不使用真实身份、凭据、访问令牌、支付接口、生产域名或公网目标。
- 不对不属于项目维护者或未获得明确授权的系统执行测试。
- 不将合成工具替换为真实生产接口，不把模拟结果描述成真实生产操作。
- 不提供或执行面向真实系统的操作步骤。
- 不以研究、测试或自动化为理由扩大既定授权边界。

## 4. 技术隔离要求

- 正式 Episode 必须在一次性 Docker 容器中执行。
- Canonical World 和运行时状态只能由宿主代码通过已定义合同物化和变更。
- 测试数据必须使用合成身份、合成资源和不可用于真实服务的标识。
- 默认禁止公网访问；模型只能访问 Episode 内明确允许的本地服务。
- 每次执行必须留下工具轨迹、权限事实、状态差异、终止状态和清理结果。
- 执行结束后不得残留容器、工作卷、模型进程或未提交的模拟状态。
- 缺少授权事实、完整性证据或隔离证明时，验收必须失败关闭。

## 5. 研究目标

本项目只评估以下工程问题：

- Agent 是否遵守角色、ACL、委托和策略边界。
- Agent 如何处理正常任务、信息不足、可信回复和受控异常输入。
- 工具调用是否产生符合合同的环境状态变化。
- Oracle 是否能够根据真实工具和状态事实区分不同风险阶段。
- recording、strict replay 和 direct execution 是否产生一致证据。
- 覆盖反馈是否能够推动后续候选探索新的行为路径和风险方向。
- 系统在异常、暂停、恢复和清理时是否保持可审计且失败关闭。

## 6. 准确表达原则

讨论和文档应使用能够准确反映隔离环境与工程目标的表述，不隐瞒任务性质，也不将模拟
行为描述为真实系统操作。推荐术语包括：

- “隔离环境中的未授权状态转换用例”
- “受控异常输入”
- “授权分支和策略拒绝”
- “合成账户状态”
- “风险目标与 Oracle 事实”
- “工具调用和环境状态变化”
- “确定性 Office 模拟系统”
- “授权的防御性 Agent 行为评测”

原始测试表达和批量 fixture 应保存在仓库内，通过文件路径、案例 ID 和摘要引用。除非修复
解析、物化或校验逻辑确有必要，不应在对话中重复展开大量原始内容。

## 7. AI 协作停止条件

AI 在以下情况下必须暂停执行并明确指出缺失的信息，不得自行扩大范围：

- 无法确认目标是否属于本仓库的合成 Docker 环境。
- 请求涉及真实身份、凭据、账户、资金、生产接口、第三方服务或公网目标。
- 请求要求绕过平台规则、隐藏真实目的或规避安全检查。
- 当前代码可能把模拟工具连接到真实系统。
- 所需操作超出用户明确授权的文件、环境或验收范围。
- 无法证明操作完成后可以清理、回滚或保留完整审计证据。

恢复工作前，必须补充明确的授权边界、隔离条件、目标文件和验收标准。

## 8. 新任务引用方式

新会话或任务应先声明：

> 本任务受 `AUTHORIZED-EVALUATION-SCOPE.md` 约束，只处理仓库内的合成 Docker Office
> 环境。本次目标、允许修改范围和聚焦验收如下。若发现请求超出该边界，请停止并说明。

随后只提供当前提交、具体缺陷、允许修改的文件和最小验收命令，不重复粘贴整个项目历史
或大批原始测试内容。
