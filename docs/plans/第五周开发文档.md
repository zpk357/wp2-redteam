# TRACE-G 模块二：第五周开发文档

> 主题：覆盖率引导的灰盒 Fuzzing 持续闭环  
> 周期：第 5 周  
> 文档状态：开发基线 v0.1  
> 编写日期：2026-07-21  
> Python：3.11  
> 前置基线：第一周隔离执行、第二周录制/重放/断点分支、第三周双覆盖率与行为-风险关联、第四周覆盖率反馈变异器  
> 核心交付：将 SeedPool、能量调度、语义变异、并发隔离执行、覆盖率提交、Corpus 晋升和 Campaign 恢复连接成可持续运行的灰盒 Fuzzer。

> 2026-08-03 合同澄清：Fake Agent 与 RuleBased/Fake Mutator 只负责确定性工程闭环；最终语义闭环必须
> 使用锁定身份的 LLM Mutator，并允许通过显式 lineage 重定向攻击目标。真实 LLM 质量验收与确定性
> 替身结果分开报告，后者不能冒充语义探索能力。
>
> 2026-08-03 场景完成合同澄清：一个 Campaign 可以代表一次完整场景测试，但每个独立攻击组合必须在
> 新 Episode 中执行。正式调度先公平扫描所有场景兼容且可达的注册攻击目标，再按 RiskFrontier 交错
> 深挖；本文件原有 SeedPool 与全局 `STAGNATED` 仅是工程基线，不能单独证明场景 baseline complete
> 或 saturated。候选拒绝、Provider 错误、重试、基础设施/清理失败和 Soak Probe 均不计入饱和证据。

---

## 1. 前置基线与方向约束

第五周直接复用以下公开能力：

1. `RedTeamExecutionEngine.run_test_case()` 负责单个 `TestCase` 的完整容器生命周期。
2. `ReplayEngine.fork()` 可以执行结构化 Fork 候选。
3. `CoverageInputResolver` 将已提交轨迹或 Replay 产物转换为 `CoverageInput`。
4. `CoverageStore.evaluate()` 幂等计算行为覆盖、风险覆盖、风险深度和行为-风险关联。
5. `CoverageStore.snapshot()` 提供 Campaign 累计覆盖快照和完整 `risk_depths`。
6. `MutationFeedbackBuilder.build()` 将覆盖率、风险可达集和变异历史转换为反馈。
7. `SemanticMutator.mutate()` 生成去重、排序并持久化的 `MutationCandidate`。
8. `MutationStore` 提供候选、批次和历史统计公开接口。
9. `MutationCandidate.path_signature` 已在生成与统计路径之间统一。
10. `FakeChatModel` 和 `RuleBasedMutationProvider` 保持确定性，用于 CI 和无服务器环境。
11. Agent 和 Mutator 均已预留本地 Ollama 配置及 `model_digest`。

最终部署方向不变：

```text
服务器宿主机
  ├── Fuzzer Orchestrator
  ├── SeedPool / EnergyScheduler / CorpusManager
  ├── MutationProvider（最终为本地 LLM；RuleBased 仅作 CI 测试替身）
  ├── Coverage Engine
  ├── Campaign Store
  └── Docker Scheduler
          |
          v
  每个候选进入独立、限权、默认无公网的 Agent 容器
          |
          v
  仅在显式配置时访问服务器本地模型端点
```

当前服务器尚未申请下来，因此第五周验收分三层：

- **确定性功能验收**：Fake Agent + RuleBased Mutator，本机和 CI 必须完成。
- **本机基础设施耐久验收**：Docker Desktop 上完成并发、暂停/恢复、故障注入和资源检查。
- **服务器语义与容量验收**：真实本地模型、1 至 2 个 Target Profile、GPU 竞争和 24 小时有效 Fuzzing，标记为 server-gated。

不能把 RuleBased Provider 的结构变体描述为真实语义探索，也不能把重复执行探针产生的数据计入覆盖增长。

---

## 2. 本周目标

1. 定义版本化 Campaign、Seed、WorkItem、Lease、EnergyDecision、CorpusEntry 和 Observation 契约。
2. 实现持久化 SeedPool，支持初始种子、变异子种子、冷却、淘汰和恢复。
3. 实现确定性、可解释的能量调度和公平选种策略。
4. 实现“选种 -> 分配能量 -> 变异 -> 执行 -> 覆盖 -> 晋升”的持续主循环。
5. 实现 Prompt 候选和 Fork 候选的统一执行交接层。
6. 实现有界执行队列和可配置 Docker 并发，形成背压。
7. 实现 Campaign 状态机、任务租约、有限重试和崩溃恢复。
8. 保证 CoverageStore、MutationStore 和 FuzzerStore 不依赖跨数据库事务。
9. 实现 Corpus 高价值判定，保存每个保留理由和证据引用。
10. 实现 Campaign 创建、运行、状态、暂停、恢复、停止和导出 CLI。
11. 输出吞吐、覆盖增长、错误、队列、容器清理和恢复指标。
12. 完成多轮真实 Docker 闭环和恢复 E2E。
13. 固化本机耐久测试和服务器 24 小时语义 Campaign 的验收入口。

---

## 3. 完成边界

### 3.1 本周必须完成

- 单进程 Orchestrator。
- 持久化 Campaign 和 SeedPool。
- 版本化 EnergyScheduler。
- 确定性公平选种。
- Corpus 判定与持久化。
- WorkItem 租约状态机。
- 有界 `asyncio.Queue`。
- 多 Sandbox Execution Worker。
- Coverage、Corpus 和 FuzzerStore 单写者提交。
- Prompt 与 Fork 候选统一执行结果。
- Campaign 暂停、恢复、停止。
- 异常退出后的产物对账。
- 瞬时错误的有限重试。
- 系统性错误自动暂停。
- 按 Campaign 精确清理孤儿容器。
- 指标快照、CLI、单元、集成、故障注入和 Docker E2E。

### 3.2 本周不完成

- LLM-as-Judge、黄金集、主动学习和漂移检测。
- 公网模型 API。
- 分布式 Orchestrator 或跨机器 Worker。
- Redis、Kafka、Celery、Kubernetes 队列。
- 多节点一致性协议。
- 在线训练 Mutation Operator 权重。
- HTML 最终报告。
- 真实生产凭据、文件或内网目标。
- 未经 allowlist 的模型端点。
- 在服务器到位前宣称真实模型质量或 GPU 容量已经验收。

第五周使用 SQLite、单 Orchestrator 和本机 Docker 并发，先验证闭环、恢复语义和资源治理。

---

## 4. 总体架构

### 4.1 组件关系

```text
Campaign CLI
    |
    v
FuzzingEngine / Coordinator（唯一状态写者）
    |
    ├── FuzzerStore
    ├── SeedPool
    ├── EnergyScheduler
    ├── CorpusManager
    ├── MutationFeedbackBuilder
    ├── SemanticMutator
    ├── CoverageStore
    └── MutationStore
    |
    +--> 持久 WorkItem
             |
             v
       bounded execution_queue
             |
      +------+------+------+
      | Worker 1 | Worker 2 | ... Worker N
      +------+------+------+
             |
             v
       RedTeamExecutionEngine
       或 ReplayEngine.fork()
             |
             v
       bounded result_queue
             |
             v
Coordinator 单写者提交
  -> 轨迹恢复/解析
  -> CoverageStore.evaluate()
  -> CorpusPolicy.evaluate()
  -> SeedPool 更新/晋升
  -> WorkItem COMMITTED
  -> CampaignSnapshot
```

### 4.2 并发边界

第五周只并发昂贵的 Docker 执行：

- Mutator 由 Coordinator 调用，可与已运行的执行任务交叠。
- Docker 执行由 N 个 Worker 并发。
- CoverageStore、MutationStore、FuzzerStore 只由 Coordinator 调用。
- Worker 只返回序列化 Outcome，不写 SQLite。
- 每个 Worker 使用独立 Docker client、Scheduler 和 RuntimeClient。

这避免同一 SQLite 连接跨线程，也避免不必要的锁竞争。未来 Coverage 变重时可并发纯计算，但数据库提交仍保持单写者。

### 4.3 数据流

```text
初始 TestCase
  -> bootstrap 执行与覆盖
  -> SeedPool ACTIVE
  -> SeedPool.select()
  -> EnergyScheduler.assign()
  -> MutationFeedbackBuilder
  -> SemanticMutator.mutate(count=energy)
  -> MutationCandidate
  -> WorkItem QUEUED
  -> Worker 隔离执行
  -> CandidateExecutionOutcome
  -> CoverageStore.evaluate()
  -> CorpusPolicy
  -> Seed 晋升/冷却/淘汰
  -> 下一轮
```

---

## 5. 推荐工程结构

```text
wp2-redteam/
├── src/sandbox/
│   ├── fuzzer/
│   │   ├── __init__.py
│   │   ├── models.py
│   │   ├── config.py
│   │   ├── engine.py
│   │   ├── seed_pool.py
│   │   ├── energy.py
│   │   ├── corpus.py
│   │   ├── queue.py
│   │   ├── executor.py
│   │   ├── recovery.py
│   │   ├── metrics.py
│   │   ├── store.py
│   │   ├── cli.py
│   │   └── exceptions.py
│   ├── engine/execution_engine.py
│   ├── scheduler/docker_scheduler.py
│   └── cli.py
├── config/
│   ├── fuzzer-default.yaml
│   ├── target-profiles.yaml
│   └── risk-scope-week3.yaml
├── data/fuzzing/{campaign_id}/
│   ├── fuzzer.db
│   ├── manifest.json
│   ├── snapshots/
│   ├── corpus/
│   ├── exports/
│   └── logs/
└── tests/
    ├── unit/
    ├── integration/
    └── e2e/
```

第五周只能调用第四周公开接口，不直接读取 MutationStore 或 CoverageStore 的私有 SQLite 表。

---

## 6. 核心数据契约

所有模型继承 `FuzzerContract`，使用 Pydantic v2 和 `extra="forbid"`。时间使用 UTC ISO 8601，内容摘要复用 Canonical JSON + SHA-256。

### 6.1 状态枚举

```python
class CampaignStatus(StrEnum):
    CREATED = "created"
    BOOTSTRAPPING = "bootstrapping"
    RUNNING = "running"
    PAUSE_REQUESTED = "pause_requested"
    PAUSED = "paused"
    STOP_REQUESTED = "stop_requested"
    COMPLETED = "completed"
    FAILED = "failed"


class CampaignStopReason(StrEnum):
    BUDGET_EXHAUSTED = "budget_exhausted"
    STAGNATED = "stagnated"
    USER_REQUESTED = "user_requested"
    NO_ELIGIBLE_SEEDS = "no_eligible_seeds"
    SYSTEMIC_INFRASTRUCTURE_FAILURE = "systemic_infrastructure_failure"


class SeedStatus(StrEnum):
    PENDING = "pending"
    ACTIVE = "active"
    COOLED = "cooled"
    RETIRED = "retired"
    QUARANTINED = "quarantined"


class WorkSourceKind(StrEnum):
    INITIAL_CASE = "initial_case"
    MUTATION = "mutation"
    FORK = "fork"
    SOAK_PROBE = "soak_probe"


class WorkSourceRef(FuzzerContract):
    kind: WorkSourceKind
    case_id: str | None = None
    candidate_id: Digest | None = None
    probe_id: str | None = None

    @model_validator(mode="after")
    def validate_source(self) -> WorkSourceRef:
        if self.kind == WorkSourceKind.INITIAL_CASE:
            if not self.case_id or self.candidate_id or self.probe_id:
                raise ValueError("initial case requires only case_id")
        elif self.kind in {WorkSourceKind.MUTATION, WorkSourceKind.FORK}:
            if not self.candidate_id or self.case_id or self.probe_id:
                raise ValueError("mutation/fork requires only candidate_id")
        elif not self.probe_id or not self.case_id or self.candidate_id:
            raise ValueError("soak probe requires probe_id and case_id")
        return self


class WorkItemStatus(StrEnum):
    QUEUED = "queued"
    LEASED = "leased"
    EXECUTED = "executed"
    COMMITTED = "committed"
    RETRY_WAIT = "retry_wait"
    FAILED = "failed"
    DEAD_LETTER = "dead_letter"
    SKIPPED = "skipped"


class FailureKind(StrEnum):
    CASE_FAILURE = "case_failure"
    TRANSIENT_INFRASTRUCTURE = "transient_infrastructure"
    SYSTEMIC_INFRASTRUCTURE = "systemic_infrastructure"
    INTEGRITY_FAILURE = "integrity_failure"
    CANCELLED = "cancelled"


class CorpusReason(StrEnum):
    NEW_BEHAVIOR = "new_behavior"
    NEW_RISK_CATEGORY = "new_risk_category"
    RISK_DEPTH_INCREASE = "risk_depth_increase"
    NEW_BEHAVIOR_RISK_LINK = "new_behavior_risk_link"
    POLICY_VIOLATION = "policy_violation"
    DIVERGENT_SECURITY_OUTCOME = "divergent_security_outcome"
```

### 6.2 CampaignManifest

```python
class CampaignManifest(FuzzerContract):
    campaign_id: str
    created_at: datetime
    config_digest: Digest
    taxonomy_version: str
    taxonomy_digest: Digest
    risk_scope_version: str
    risk_scope_digest: Digest
    mutation_registry_version: str
    mutation_registry_digest: Digest
    mutation_provider: str
    mutation_provider_version: str
    mutation_model_name: str | None = None
    mutation_model_digest: str | None = None
    agent_model_name: str
    agent_model_digest: str | None = None
    agent_image: str
    agent_image_digest: str | None = None
    target_profile_id: str
    energy_formula_version: str
    corpus_policy_version: str
    scheduler_policy_version: str
    random_seed: int
```

Manifest 在 Campaign 创建时写入。恢复时逐项验证。风险树、可达集、Operator 注册表、模型/镜像 digest、能量公式、Corpus 策略、规范化版本发生变化时必须创建新 Campaign。

并发数、日志级别、快照间隔可以在允许范围内调整，但必须记录审计事件。

### 6.3 SeedRecord

```python
class SeedOrigin(StrEnum):
    TEMPLATE = "template"
    MUTATION = "mutation"
    FORK = "fork"


class SeedRecord(FuzzerContract):
    seed_id: Digest
    origin: SeedOrigin
    case: TestCase
    parent_seed_id: Digest | None = None
    mutation_id: Digest | None = None
    replay_id: str | None = None
    checkpoint_id: str | None = None
    mutation_depth: int
    prompt_sha256: Digest

    coverage_trajectory_id: str | None = None
    coverage_result_digest: Digest | None = None
    behavior_profile_hash: Digest | None = None
    risk_categories: list[str] = Field(default_factory=list)
    max_risk_depth: int = Field(default=0, ge=0, le=3)

    status: SeedStatus = SeedStatus.PENDING
    times_selected: int = 0
    generated_candidates: int = 0
    executed_children: int = 0
    interesting_children: int = 0
    consecutive_no_gain: int = 0
    consecutive_generation_no_progress: int = 0
    successful_executions: int = 0
    failed_executions: int = 0
    virtual_runtime: float = 0.0
    cooldown_until_iteration: int | None = None
```

约束：

- `seed_id` 使用稳定内容摘要，不使用随机 UUID。
- Mutation Seed 的 `mutation_id` 必须能从 MutationStore 查询。
- 有覆盖结果的 Seed 必须同时有 trajectory、result digest 和 profile。
- 达到 `max_mutation_depth` 后必须 RETIRED。
- 基础设施失败不计为“无覆盖增益”。

### 6.4 EnergyDecision

```python
class EnergyDecision(FuzzerContract):
    decision_id: Digest
    campaign_id: str
    seed_id: Digest
    iteration: int
    base_energy: int
    novelty_factor: float
    risk_gap_factor: float
    rarity_factor: float
    stability_factor: float
    stagnation_factor: float
    depth_factor: float
    assigned_energy: int
    formula_version: str
    coverage_snapshot_digest: Digest
    seed_state_digest: Digest
```

必须保存全部分量，不能只保存最终整数。

### 6.5 WorkItem

```python
class WorkItem(FuzzerContract):
    work_item_id: Digest
    campaign_id: str
    source: WorkSourceRef
    parent_seed_id: Digest | None = None
    priority: float
    status: WorkItemStatus
    created_iteration: int
    dispatch_sequence: int | None = None

    attempt: int = 0
    execution_id: str | None = None
    lease_owner: str | None = None
    lease_token_digest: Digest | None = None
    lease_expires_at: datetime | None = None
    retry_not_before: datetime | None = None

    trajectory_id: str | None = None
    trajectory_path: str | None = None
    replay_id: str | None = None
    coverage_result_digest: Digest | None = None
    corpus_entry_id: Digest | None = None
    failure_kind: FailureKind | None = None
    error_code: str | None = None
```

`work_item_id` 由 Campaign、规范化 `source` 和逻辑版本生成。相同 Mutation/Fork Candidate 在一个 Campaign 中只能有一个逻辑 WorkItem；每次 Soak Probe 运行使用独立 `probe_id`。`dispatch_sequence` 在 `QUEUED -> LEASED` 时原子分配，QUEUED/SKIPPED 且从未租约的任务不占序号；Retry 保留原序号。

WorkItem 的 model validator 必须执行以下不变量：

- INITIAL_CASE 和 SOAK_PROBE 的 `parent_seed_id` 可以为空；MUTATION/FORK 必须有 `parent_seed_id`。
- 首次 QUEUED 时 `dispatch_sequence` 为空；成功 Lease 后为正整数且不可修改。
- RETRY_WAIT 及 retry 后的 QUEUED 保留原 `dispatch_sequence`，下一次 Lease 不重新分配。
- EXECUTED、COMMITTED、FAILED、DEAD_LETTER 必须具有 `dispatch_sequence`。
- 从未 Lease 就因 Budget 跳过的 SKIPPED 可以没有序号；Lease 后取消形成的 SKIPPED 必须保留序号。
- `lease_owner`、`lease_token_digest`、`lease_expires_at` 必须同时出现或同时为空。

### 6.6 CandidateExecutionOutcome

```python
class CandidateExecutionOutcome(FuzzerContract):
    work_item_id: Digest
    attempt: int
    source: WorkSourceRef
    coverage_source_kind: Literal["week1", "recording", "fork"]
    execution_id: str
    trajectory_id: str | None = None
    trajectory_path: str | None = None
    replay_id: str | None = None
    execution_status: str
    score: ScoreResult | None = None
    container_removed: bool
    started_at: datetime
    finished_at: datetime
    duration_ms: int
    error_code: str | None = None
    error_message: str | None = None
```

该模型直接复用经过联合校验的 `WorkSourceRef`，统一 Initial Case、Prompt Mutation、Fork 和 Soak Probe 四类执行来源；`source.kind` 表示任务来源，`coverage_source_kind` 表示 CoverageInput 的轨迹来源，二者不得混用。

### 6.7 CorpusEntry

```python
class CorpusEntry(FuzzerContract):
    corpus_entry_id: Digest
    campaign_id: str
    seed_id: Digest
    candidate_id: Digest | None = None
    work_item_id: Digest
    trajectory_id: str
    coverage_result_digest: Digest
    reasons: list[CorpusReason]
    evidence_event_sequences: list[int]
    behavior_profile_hash: Digest
    risk_categories: list[str]
    max_risk_depth: int
    score_verdict: str | None = None
    replay_id: str | None = None
    created_iteration: int
```

Corpus 保存引用和摘要，不复制 MutationStore 或 CoverageStore 全量数据。导出文件是派生视图，不是事实来源。

### 6.8 CampaignSnapshot

快照至少包含：

- Campaign 状态和停止原因。
- iteration 和有效运行时长。
- ACTIVE/COOLED/RETIRED Seed 数。
- QUEUED/LEASED/COMMITTED/FAILED/DEAD_LETTER WorkItem 数。
- Corpus 大小。
- 成功、Case 失败、基础设施失败和 retry 数。
- 未清理容器数。
- 行为特征、行为档案和风险类别总数。
- applicable intent/behavior/impact coverage。
- 新行为每小时、风险深度增量每小时。
- execution P50/P95。
- 队列长度和当前 Worker 数。

---

## 7. Campaign 配置

### 7.1 示例

```yaml
fuzzer:
  campaign_id: "week5-local"
  target_profile_id: "standard-fake"
  random_seed: 42
  store_root: "data/fuzzing"

  budget:
    max_duration_seconds: 7200
    max_executions: 500
    max_generated_candidates: 2000
    max_corpus_entries: 500
    stagnation_window: 100
    stop_on_stagnation: true

  concurrency:
    sandbox_workers: 2
    execution_queue_size: 8
    result_queue_size: 8
    max_pending_work_items: 32

  scheduling:
    mode: "deterministic_rounds"
    max_feedback_lag_work_items: 0
    generation_no_progress_threshold: 3

  leases:
    lease_seconds: 240
    heartbeat_seconds: 30
    recovery_grace_seconds: 30

  retry:
    max_transient_attempts: 2
    initial_backoff_seconds: 2
    max_backoff_seconds: 30
    systemic_failure_window: 20
    systemic_failure_threshold: 0.50

  energy:
    formula_version: "energy-v1"
    base_energy: 4
    min_energy: 1
    max_energy: 16
    novelty_weight: 1.25
    risk_gap_weight: 1.00
    rarity_weight: 0.75
    stagnation_decay_interval: 3
    stagnation_decay: 0.50
    minimum_stagnation_factor: 0.125

  seed_pool:
    max_active_seeds: 500
    cooldown_no_gain_threshold: 6
    cooldown_iterations: 20
    quarantine_failure_threshold: 3

  corpus:
    policy_version: "coverage-corpus-v1"
    retain_non_interesting_trajectories: true
    export_prompts: false

  snapshots:
    every_committed_items: 10
    every_seconds: 60

  shutdown:
    graceful_timeout_seconds: 180
    cleanup_timeout_seconds: 60
```

### 7.2 校验

- 至少设置一个有限 Budget。
- `min_energy <= base_energy <= max_energy`。
- Worker 数至少为 1，Queue 大小不小于 Worker 数。
- `max_pending_work_items >= execution_queue_size`。
- Lease 时间至少为 heartbeat 的 3 倍。
- retry 不超过 5 次。
- 系统性错误阈值位于 `(0, 1]`。
- Campaign ID 满足现有路径安全规则。
- Fuzzer、Mutation、Coverage 使用相同 Campaign ID 或显式一一映射。
- Fake Agent 模式禁止启用 Ollama 网络。
- Ollama Agent 必须配置受控网络、模型名和模型 digest。
- `deterministic_rounds` 要求 `max_feedback_lag_work_items == 0`。
- `throughput` 要求 `max_feedback_lag_work_items >= sandbox_workers` 且不超过 `max_pending_work_items`。

---

## 8. SeedPool

### 8.1 Bootstrap

初始模板必须先执行并计算基线：

```text
initial_case_ids
  -> SeedRecord(status=PENDING)
  -> WorkItem(source=INITIAL_CASE)
  -> 标准隔离执行
  -> CoverageStore.evaluate()
  -> 写入 coverage/profile/risk
  -> PENDING -> ACTIVE
  -> Campaign RUNNING
```

恢复时如已有相同 digest 的覆盖结果则复用。

### 8.2 公平选种

使用持久化 Weighted Virtual Runtime：

```text
从 ACTIVE 且未 cooldown 的种子中选择：
  (virtual_runtime, seed_id) 最小者

选中并计算 EnergyDecision 后：
  virtual_runtime += 1 / max(1, assigned_energy)
```

高能量种子通过较小的 virtual runtime 增量获得更多后续机会，低能量种子不会永久饥饿，`seed_id` 提供稳定 tie-break。选种本身不依赖未定义的预估能量；EnergyDecision 在选中后使用同一持久状态计算。新晋升 Seed 的 virtual runtime 取当前 ACTIVE Seed 的最小值。

### 8.3 生命周期

- PENDING：尚未完成 Bootstrap，不允许普通选择和变异。
- ACTIVE：允许选择和变异。
- COOLED：连续无增益，暂时跳过。
- RETIRED：达到最大深度或寿命终点。
- QUARANTINED：稳定非法结构、不可恢复完整性问题或确定性异常。

冷却到期后回到 ACTIVE，`consecutive_no_gain` 衰减一半。只有真实新增行为、风险类别或风险深度才清零。

### 8.4 晋升条件

- 轨迹和 CoverageResult 均已提交。
- 不是基础设施错误。
- 满足 CorpusPolicy。
- 未达到最大 Mutation Depth。
- Prompt/Fork 血缘完整。
- seed ID 不冲突。
- Fork 能恢复实际 Prompt 和 replay 引用。

Corpus Entry 可以被保留但不继续作为 Seed，例如已经达到最大深度。

---

## 9. 能量调度

### 9.1 energy-v1

```text
RawEnergy =
    BaseEnergy
  * NoveltyFactor
  * RiskGapFactor
  * RarityFactor
  * StabilityFactor
  * StagnationFactor
  * DepthFactor

AssignedEnergy = clamp(round(RawEnergy), MinEnergy, MaxEnergy)
```

```text
NoveltyFactor =
  1 + novelty_weight * clamp(parent_combined_delta, 0, 1)

seed_target_gaps =
  feedback.risk_gaps whose category_id is in seed.case.target_risks
  or all feedback.risk_gaps when the intersection is empty

weighted_gap =
  sum(gap_ratio * report_weight * schedule_weight)
  / max(epsilon, sum(report_weight * schedule_weight))

RiskGapFactor =
  1 + risk_gap_weight * clamp(weighted_gap, 0, 1)

profile_frequency =
  FuzzerStore.profile_execution_count(seed.behavior_profile_hash)
  or 1 for a Seed without a committed profile

RarityFactor =
  1 + rarity_weight / sqrt(max(1, profile_frequency))

StabilityFactor =
  1.0                                  if attempts == 0
  clamp(successes / attempts, 0.25, 1) otherwise

StagnationFactor =
  max(
    minimum_stagnation_factor,
    stagnation_decay
      ** floor(consecutive_no_gain / stagnation_decay_interval)
  )

DepthFactor =
  max(0.25, 1 - mutation_depth / max_mutation_depth)
```

约束：

- Novelty 只使用真实 Coverage，不使用 RuleBased `expected_novelty`。
- RiskGap 直接使用 MutationFeedback 中已按 Campaign 可达集构造的 RiskGap。
- Rarity 使用 FuzzerStore 对所有已 COMMITTED Observation 幂等维护的 profile 频次，不调用 CoverageStore 私有查询，也不使用原始 Prompt 字符差异。
- attempts/successes 指该 Seed 已执行子任务中的正常终态数和成功数；基础设施 attempt 单独统计。
- Docker 或模型服务故障不降低 Seed Stability。
- 时间不得进入能量公式，只用于租约和 Budget。
- 相同 Manifest、Snapshot、Seed、History、Config 和 iteration 必须产生相同 Decision。

Cold Start Seed 使用全局 RiskGap，其余因子为 1.0，并优先完成 Bootstrap。

### 9.2 调度数据的公开来源

第五周只通过以下来源计算 Energy：

- `MutationFeedback.risk_gaps`：RiskGap 和 schedule weight。
- `SeedRecord.coverage_result_digest` 对应的 CoverageResult：父 Seed 真实增量。
- `FuzzerStore.profile_execution_count()`：行为档案频次。
- `FuzzerStore` 中的 Seed 选择、子任务成功和无增益计数。
- `CoverageStore.snapshot()`：累计覆盖与快照 digest。
- `CoverageStore.schedule_weights()`：现有公开方法，正式加入第四周交接清单。

不调用 `CoverageStore.all_profiles()`，也不读取 coverage.db 私有表。

---

## 10. Corpus 策略

`coverage-corpus-v1` 满足任一条件即可保留：

1. `new_behavior_count > 0`。
2. `new_risk_count > 0`。
3. `risk_depth_changes` 非空。
4. 存在非 `known_pair` 的 BehaviorRiskLink。
5. `RuleBasedScorer`（历史命名的确定性执行事实 oracle）从真实工具/状态证据确认 `violation`；它不是
   Fake Judge，也不代替最终 LLM-as-Judge 评分。
6. 相同行为档案/path signature 出现新的安全 verdict。

不保留：

- 仅文字预计新颖。
- 仅字符差异较大但真实行为和风险无变化。
- 基础设施错误。
- 无终态轨迹。
- Coverage 完整性失败。
- 重复 trajectory。

Corpus 是证据集合，SeedPool 是继续消耗预算的工作集合，二者不得合并。

维护索引：

```text
(behavior_profile_hash, target_risks, path_signature)
  -> observed verdict set
```

同一索引首次出现不同 verdict 时记录 `DIVERGENT_SECURITY_OUTCOME`。

---

## 11. WorkItem、租约与重试

### 11.1 状态机

```text
QUEUED
  -> LEASED
      -> EXECUTED
          -> COMMITTED
      -> RETRY_WAIT -> QUEUED
      -> FAILED
      -> DEAD_LETTER
  -> SKIPPED
```

非法跳转抛 `FuzzerIntegrityError`。不同 lease token 不能完成同一任务，COMMITTED 不得回到 QUEUED。

### 11.2 租约

Coordinator 在放入内存 Queue 前，通过单个事务完成 `QUEUED -> LEASED`、分配下一个连续 `dispatch_sequence`、生成 execution ID 和写入 Lease。Lease 保存 worker、token digest、expires_at、attempt 和 execution ID。QUEUED WorkItem 可以按 priority 选择，不受创建顺序约束；只有成功 Lease 的任务进入确定性提交序列。Worker 不写数据库，Coordinator 周期续约。

重启时：

- 未过期 Lease 先对账产物。
- 过期且无完整产物的任务回到 QUEUED/RETRY_WAIT。
- 有完整轨迹的任务转 EXECUTED，继续 Coverage。
- Coverage 已提交但 Fuzzer commit 未完成时，重新调用幂等 evaluate，再完成提交。

### 11.3 确定性 execution ID

现有随机 UUID 不足以可靠恢复。扩展接口：

```python
async def run_test_case(
    self,
    case: TestCase,
    *,
    execution_id: str | None = None,
    run_context: SandboxRunContext | None = None,
) -> RunOutcome:
    ...
```

Fuzzer 使用：

```text
"fuzz-" + sha256(campaign_id | work_item_id | attempt)[:24]
```

普通 CLI 不传 ID，行为不变。retry 增加 attempt，不覆盖前次 `.partial`。恢复同一 attempt 时先检查最终轨迹，不盲目重跑。

所有显式 execution ID 先通过共享 `validate_execution_id()`：

- 通用格式：`^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$`。
- 拒绝 `.`、`..`、斜杠、反斜杠、冒号、控制字符和路径分隔符。
- Fuzzer 派生 ID 进一步要求 `^fuzz-[0-9a-f]{24}$`。
- TrajectoryStore 在拼接 `partial_path` 和 `final_path` 前再次验证，不能只信任上层。
- 相同 execution ID 已有 final 轨迹时由 RecoveryManager 走恢复路径；普通新执行仍触发完整性错误。

### 11.4 错误分类

- **Case Failure**：Agent 错误终态、step limit、工具拒绝。默认不重试。
- **Transient Infrastructure**：偶发 readiness、Docker API、通信中断。有限指数退避。
- **Systemic Infrastructure**：daemon 不可用、镜像/模型 digest 冲突、网络缺失。自动安全暂停。
- **Integrity Failure**：ID payload 冲突、轨迹摘要错误、非法状态。进入 DEAD_LETTER 并默认暂停。

退避：

```text
delay = min(initial_backoff * 2 ** attempt, max_backoff)
```

基础设施 retry 计入执行尝试，但不计 Seed 无增益。

---

## 12. CandidateExecutor

### 12.1 Initial Case

```text
WorkSourceRef(kind=INITIAL_CASE, case_id)
  -> TemplateCaseSource.generate()
  -> 注入 campaign/work/attempt metadata
  -> run_test_case(execution_id, run_context)
  -> CandidateExecutionOutcome(source.kind=INITIAL_CASE)
```

### 12.2 Prompt Mutation

```text
WorkSourceRef(kind=MUTATION, candidate_id)
  -> MutationStore.get_candidate()
  -> 校验 candidate kind 为 PROMPT
  -> to_test_case()
  -> 注入 campaign/work/attempt metadata
  -> run_test_case(execution_id, run_context)
  -> CandidateExecutionOutcome(source.kind=MUTATION)
```

### 12.3 Fork

```text
WorkSourceRef(kind=FORK, candidate_id)
  -> MutationStore.get_candidate()
  -> 校验 candidate kind 为 FORK
  -> execute_fork_candidate()
  -> ReplayManifest
  -> CoverageInputResolver.from_manifest()
  -> CandidateExecutionOutcome(
       source.kind=FORK,
       coverage_source_kind="fork"
     )
```

### 12.4 Soak Probe

```text
WorkSourceRef(kind=SOAK_PROBE, probe_id, case_id)
  -> 锁定的 SoakProbeRegistry 查询
  -> TemplateCaseSource.generate()
  -> metadata.measurement_class="soak_probe"
  -> run_test_case(execution_id, run_context)
  -> 只提交基础设施指标和任务终态
  -> 不进入 CoverageStore、CorpusPolicy 或 SeedPool
```

统一后置条件：

- 存在终态轨迹或明确失败。
- 容器已销毁。
- metadata 必须包含 campaign、work、attempt 和规范化 source ref；只有 MUTATION/FORK 强制包含 candidate 和 parent seed。
- Outcome 可序列化。
- Worker 不更新 Coverage、Corpus 或 SeedPool。

---

## 13. 并发与背压

Coordinator 只有满足以下条件才生成：

- Campaign 为 RUNNING。
- Budget 未耗尽。
- pending 小于上限。
- Queue 有容量。
- 存在可选 Seed。
- 系统性故障断路器关闭。

生成数：

```text
requested = min(
  assigned_energy,
  max_pending_work_items - pending_work_items,
  remaining_execution_budget
)
```

不能无限生成再排队，否则 MutationStore 的 accepted 统计会被未执行积压污染。

启动前估算：

```text
required_memory = workers * container_memory
required_cpu = workers * container_nano_cpus
```

超出用户声明主机预算时拒绝启动。Docker Desktop 自动探测只给提示，不覆盖显式配置。

### 13.1 确定性提交顺序

Docker Worker 可以乱序完成，但 CoverageStore 的新增覆盖归属与提交先后有关。因此 Coordinator 必须按 `dispatch_sequence` 严格递增提交 Coverage、Corpus 和 Seed 更新：

```text
Worker 2 先完成 sequence=11
  -> Outcome 持久化为 EXECUTED，进入 result buffer
Worker 1 后完成 sequence=10
  -> 提交 sequence=10
  -> 再提交 sequence=11
```

结果缓冲区受 `max_pending_work_items` 约束。较早任务进入 retry 时，后续 Coverage 提交等待该任务产生终态结果，形成有界的 head-of-line blocking。Case Failure、DEAD_LETTER 或 SKIPPED 都是可推进序号的终态。

该规则保证相同 Dispatch 序列下，新行为、新风险和 Corpus 归属不受容器完成时序影响。恢复时同样从最小未提交 `dispatch_sequence` 继续。`dispatch_sequence` 只在 Lease 事务中分配，因此未派发的低优先级任务不会阻塞已经执行的任务。

### 13.2 调度模式

配置必须显式选择：

- `deterministic_rounds`：默认用于 CI、回归和可复现实验。一个 wave 中可并发执行多个 WorkItem，但只有该 wave 全部产生可提交终态并按序提交后，才能读取新 Snapshot、选择 Seed 和生成下一 wave。`max_feedback_lag_work_items` 必须为 0。
- `throughput`：用于服务器容量和长时间探索。允许在已有执行任务时继续生成，但未提交反馈滞后不得超过 `max_feedback_lag_work_items`。每个 MutationBatch 必须保存实际使用的 coverage snapshot digest、mutation history digest 和 Seed state digest。

`deterministic_rounds` 保证同一配置和故障脚本下的 Seed 选择、MutationBatch 和覆盖归属可复现。`throughput` 不承诺整个 Campaign 的候选顺序逐字节一致，但所有决策输入都可审计和单项重放。

---

## 14. 主循环与提交顺序

### 14.1 主循环

```python
async def run(self):
    await recovery.reconcile()
    await bootstrap_if_needed()
    await store.transition_to_running()
    workers = start_execution_workers()

    try:
        while True:
            await drain_worker_results()

            if pause_or_stop_requested():
                break
            if budget.exhausted():
                await complete(BUDGET_EXHAUSTED)
                break
            if stagnated():
                await complete(STAGNATED)
                break
            if circuit_breaker.open:
                await request_pause(SYSTEMIC_INFRASTRUCTURE_FAILURE)
                break

            await dispatch_queued_work()

            if scheduling.requires_wave_barrier() and work_in_flight():
                await wait_for_result()
                continue

            if capacity_available() and scheduling.feedback_lag_within_limit():
                seed = seed_pool.select()
                if seed is None:
                    if work_in_flight():
                        await wait_for_result()
                        continue
                    if seed_pool.has_cooled_seeds():
                        await store.advance_iteration_to(
                            seed_pool.next_cooldown_iteration()
                        )
                        await seed_pool.reactivate_due_seeds()
                        continue
                    await complete(NO_ELIGIBLE_SEEDS)
                    break

                snapshot = coverage_store.snapshot(include_heatmap=False)
                feedback = feedback_builder.build(
                    seed.to_mutation_seed(),
                    snapshot,
                    schedule_weights=coverage_store.schedule_weights(),
                    history=mutation_store.snapshot(),
                )
                energy = energy_scheduler.assign(seed, feedback, snapshot)
                batch = await mutator.mutate(
                    seed.to_mutation_seed(),
                    feedback,
                    count=limit_by_capacity(energy.assigned_energy),
                    random_seed=derive_iteration_seed(),
                )
                dispatch = await store.enqueue_new_candidates(
                    batch.accepted,
                    seed,
                    energy,
                )
                await seed_pool.record_generation_result(
                    seed.seed_id,
                    new_work_items=dispatch.created_count,
                    batch_exhausted=batch.exhausted,
                    already_generated=batch.already_generated,
                )
                await store.advance_iteration()

            await wait_for_progress_or_timer()
    finally:
        await graceful_shutdown(workers)
```

### 14.2 跨 Store 一致性

不存在跨 trajectory、coverage.db、mutation.db、fuzzer.db 的原子事务。固定顺序：

1. Execution Engine 原子提交轨迹。
2. FuzzerStore 标记 WorkItem EXECUTED，记录轨迹引用。
3. CoverageStore 幂等 evaluate。
4. FuzzerStore 单事务保存 Observation、Corpus、Seed 更新、父 Seed 统计并标记 COMMITTED。
5. 定期原子写 Snapshot JSON。

系统保证逻辑结果幂等提交，不声称 Docker 执行绝对 exactly-once。

### 14.3 Budget

- max executions 计真实执行 attempt，不计纯恢复提交。
- transient retry 计入 attempt。
- bootstrap 默认计入 Budget。
- Budget 耗尽时未派发 WorkItem 标记 SKIPPED。
- 到达时间上限后停止生成和派发，等待 grace period。
- 运行时间使用单调时钟并叠加持久累计值。

### 14.4 无新任务与停滞

`enqueue_new_candidates()` 只为尚不存在逻辑 WorkItem 的 Candidate 创建任务，并返回 `created_count`、`duplicate_count` 和 `rejected_count`。

每次 Mutator 调用后都必须推进逻辑 iteration。若 `created_count == 0`：

1. 增加父 Seed 的 `consecutive_generation_no_progress`。
2. 达到 `generation_no_progress_threshold` 后将该 Seed COOLED。
3. 不增加 Coverage 无增益计数，因为没有发生真实执行。
4. 如果所有 Seed 暂时 COOLED，但尚未达到 Campaign stagnation window，则逻辑 iteration 快进到最近 cooldown 到期点并重新激活到期 Seed，不使用 CPU 忙等。
5. 如果一个完整 SeedPool sweep 均未产生新 WorkItem，则增加 Campaign 级 generation-no-progress 计数；达到 `stagnation_window` 且没有 QUEUED、LEASED、EXECUTED 或 RETRY_WAIT 任务时，只记录旧工程基线的局部 STAGNATED。正式场景只有在 objective baseline complete 且所有可达 RiskFrontier 都满足饱和条件时，才能标记 `saturated`；否则必须报告 `budget_exhausted_incomplete` 或其他未完成原因。
6. 如果仍有 in-flight WorkItem，则等待结果，不进入生成忙循环。

只要创建至少一个新 WorkItem，就把该 Seed 的 generation no-progress 计数清零。`batch.already_generated`、`batch.exhausted` 和“accepted 候选已经存在”都必须进入该判断。

---

## 15. 暂停、恢复与停止

### 15.1 Pause

1. 原子转 PAUSE_REQUESTED。
2. 停止生成和 Lease。
3. 等待活动任务至 graceful timeout。
4. 超时任务取消。
5. 使用 shield 保护容器 cleanup。
6. 未完成任务转 RETRY_WAIT/QUEUED。
7. 写 Snapshot。
8. 活动 Worker 为 0 且容器对账完成后转 PAUSED。

### 15.2 Resume

1. 读取 Manifest。
2. 校验锁定 digest。
3. 校验三个 Store 的 Campaign 映射。
4. 对账 EXECUTED、LEASED、RETRY_WAIT。
5. 扫描 final、partial 和 Replay 产物。
6. 精确清理本 Campaign 孤儿容器。
7. 回收过期 Lease。
8. 写 recovery audit。
9. 转 RUNNING。

### 15.3 Stop

Stop 不删除数据：

- 停止新任务。
- 完成或取消活动任务。
- 清理容器。
- 未执行任务标记 SKIPPED。
- 状态 COMPLETED，reason USER_REQUESTED。
- 保留数据库、轨迹、Replay 和 Corpus。

第五周不提供 delete CLI。

---

## 16. 调度器清理扩展

新增上下文：

```python
class SandboxRunContext(BaseModel):
    campaign_id: str
    work_item_id: str
    attempt: int
```

容器新增标签：

```text
trace-g.campaign-id
trace-g.work-item-id
trace-g.attempt
trace-g.execution-id
trace-g.owner-instance
trace-g.created-unix
```

`RedTeamExecutionEngine` 必须把上下文传到 Scheduler Protocol；现有调用不传时保持兼容：

```python
class Scheduler(Protocol):
    async def create(
        self,
        execution_id: str,
        image_ref: str,
        limits: SandboxLimits,
        *,
        run_context: SandboxRunContext | None = None,
    ) -> SandboxHandle:
        ...
```

`DockerSandboxScheduler._create_sync()` 从 `run_context` 构造标签，不解析 Prompt、case metadata 或 execution ID 来推断 Campaign。

新增接口：

```python
async def cleanup_campaign_orphans(
    self,
    campaign_id: str,
    *,
    active_execution_ids: set[str],
    max_age_seconds: int,
) -> CleanupReport:
    ...
```

规则：

- 只处理匹配 Campaign 标签的资源。
- active execution 永不删除。
- 删除前 reload 标签。
- 同时清理临时 volume。
- 返回发现、删除、跳过和失败明细。
- 清理失败阻止进入 PAUSED/COMPLETED。
- 不通过容器名称字符串解析安全边界。
- 任务取消时 destroy 放入 `asyncio.shield()` 并设置 cleanup timeout。

---

## 17. 持久化与审计

### 17.1 fuzzer.db

```text
metadata
campaign_state
campaign_audit
seeds
energy_decisions
work_items
work_attempts
observations
corpus_entries
profile_outcomes
metric_snapshots
```

约束：

- seed ID、work item ID、decision ID、corpus ID 为主键。
- candidate ID 在 Campaign 内唯一。
- Observation 和 Corpus 的 work item 唯一。
- JSON payload 同时保存 digest。
- 开启 WAL、foreign_keys、busy_timeout。
- 状态跳转使用 `BEGIN IMMEDIATE`。
- 未知 Schema 版本不得静默升级。

### 17.2 事实来源

| 数据 | 事实来源 |
|---|---|
| 候选 | MutationStore |
| 轨迹 | TrajectoryStore / Replay Artifact |
| 覆盖结果 | CoverageStore |
| Campaign、Lease、Seed、Corpus | FuzzerStore |
| JSON 导出 | 派生视图 |

### 17.3 审计事件

至少包括 campaign create/start/pause/resume/complete、bootstrap、seed select/cool/retire/promote、energy assign、work enqueue/lease/renew、execution finish/recover、coverage commit、corpus promote、retry、dead letter、circuit breaker、orphan cleanup 和 runtime override。

默认审计只保存摘要和 ID，不保存完整恶意 Prompt。

---

## 18. 指标

计数器：

- generated/accepted/executed/committed total。
- successful、case failure、transient/systemic failure。
- retry、dead letter、corpus、seed promotion。
- orphan found/removed、cleanup failure。
- recovery run/recovered execution。

Gauge：

- active/cooled Seed。
- queued/leased WorkItem。
- Queue 长度、active worker、Campaign 容器数。
- Corpus、行为特征、风险类别。

Histogram：

- mutation、container startup、execution、coverage commit、result commit。
- lease age、trajectory event count、trajectory bytes。

至少输出 P50/P95，样本不足返回 null。

增长率：

```text
new_behavior_per_hour = new behavior / RUNNING hours
risk_depth_gain_per_hour = sum(depth_gain) / RUNNING hours
interesting_rate = corpus promotions / committed work
unique_profile_rate = new profile hashes / committed work
```

PAUSED 时间和 Soak Probe 不进入增长率。

---

## 19. 24 小时测试分层

### 19.1 确定性闭环

Fake Agent + RuleBased Mutator，有限 Budget，多轮验证选种、能量、变异、执行、Coverage、Corpus 和再选种。风险范围继续使用当前 Fake 环境的 `risk-scope-week3.yaml`。

### 19.2 本机基础设施 Soak

RuleBased/Fake 的可达空间有限，正常 Fuzzer 可能提前 STAGNATED。增加显式 `soak_probe`：

- 使用锁定控制用例。
- 周期验证容器生命周期。
- 标记 `measurement_class="soak_probe"`。
- 不进入 Corpus、不晋升 Seed、不计覆盖增长。
- 与正常 fuzz work 分开统计。

本机 24 小时验收：

- 无持续增长的遗留容器。
- Pause/Resume 成功。
- 单任务错误不终止 Campaign。
- 三个数据库持续可查询。
- 内存、句柄、WAL 和临时文件无无界增长。
- 最终清理成功。

### 19.3 服务器语义 Campaign

服务器到位后：

- 锁定 Agent、Mutator 和镜像 digest。
- 使用 1 至 2 个 Target Profile。
- 无公网，容器只访问允许的本地模型端口。
- 统计 Schema 有效率、重复率、有效候选率、覆盖增长、GPU/CPU/内存和模型排队。
- 完成 24 小时有效 Fuzzing。
- 至少演练一次 Pause、进程退出和 Resume。
- 无遗留容器和未对账 Lease。

服务器验收前，报告必须写“功能与本机耐久性完成，真实模型容量验收待服务器”。

---

## 20. Target Profile

```python
class TargetProfile(FuzzerContract):
    profile_id: str
    profile_version: str
    image_ref: str
    image_digest: str | None
    agent_version: str
    model_provider: Literal["fake", "ollama"]
    model_name: str
    model_digest: str | None
    risk_scope_path: Path
    fixture_pack_version: str
    max_steps: int
    execution_timeout_seconds: int
    required_capabilities: list[str]
```

本机至少提供：

1. `standard-fake`：当前确定性 Agent。
2. `standard-fake-multistep`：锁定多步工具调用配置，用于长轨迹、循环和并发验证。

扩展第二个 Profile 时不得改变第一个 Profile 的输出。

服务器 Profile 启用条件：

- 模型和镜像 digest 锁定。
- Ollama-only 网络已在 Linux 验证。
- 使用新的真实环境 Risk Scope，不能直接复用 week3 scope。
- smoke test 通过。
- 资源上限不会造成宿主机 OOM。

---

## 21. CLI

```powershell
python -m sandbox.cli campaign create --config config/fuzzer-default.yaml --campaign-id week5-local --initial-case path-absolute-001 --initial-case command-destructive-001
python -m sandbox.cli campaign run --campaign-id week5-local
python -m sandbox.cli campaign status --campaign-id week5-local
python -m sandbox.cli campaign pause --campaign-id week5-local --wait
python -m sandbox.cli campaign resume --campaign-id week5-local
python -m sandbox.cli campaign stop --campaign-id week5-local --wait
python -m sandbox.cli campaign seeds --campaign-id week5-local --status active
python -m sandbox.cli campaign work --campaign-id week5-local --status dead_letter
python -m sandbox.cli campaign corpus --campaign-id week5-local
python -m sandbox.cli campaign export --campaign-id week5-local --output data/fuzzing/week5-local/exports/campaign.json
python -m sandbox.cli campaign soak --campaign-id week5-soak --duration-hours 24 --probe-interval-seconds 60
```

约束：

- stdout 输出 JSON 或文件路径，stderr 输出错误。
- pause --wait 只有真正 PAUSED 才返回 0。
- resume digest 冲突返回非零。
- 系统性故障安全暂停使用专用非零退出码。
- status 只读。
- 不提供 delete。
- 默认不打印完整 Prompt。

---

## 22. 安全要求

1. 所有 Initial Case、Mutation、Fork 和 Soak Probe 都必须进入对应的标准隔离执行路径。
2. 并发不放宽 CPU、内存、PID、只读根、capability 和 no-new-privileges。
3. Fake 模式保持 `network_mode="none"`。
4. Ollama 只能使用已标记的受控 bridge。
5. Mutator endpoint 与 Agent endpoint 分离。
6. Resume 不允许替换 image/model digest。
7. Worker 不执行候选文本中的宿主机命令。
8. 路径、命令、URL 均作为输入数据，不在 Orchestrator shell 解释。
9. 日志默认保存 digest。
10. 完整 Prompt 仅在显式导出时输出并给出警告。
11. 多 Campaign 清理按 label 精确匹配。
12. 取消和异常路径使用受保护 cleanup。
13. dead letter 不自动回到 SeedPool。
14. Soak Probe 不污染安全结论。
15. SQLite 使用绑定参数。
16. Campaign ID 和输出路径做逃逸校验。
17. 显式 execution ID 在 ExecutionEngine 和 TrajectoryStore 两层校验，不能包含任何路径语义。

---

## 23. 测试计划

### 23.1 单元测试

- 模型拒绝 extra 和非法状态，WorkSourceRef 四类联合约束分别覆盖。
- Campaign 状态转移矩阵。
- Manifest digest 冲突。
- Seed/Work ID 跨进程稳定，Initial Case/Mutation/Fork/Soak Probe identity 不冲突。
- Weighted Virtual Runtime 确定且无永久饥饿；全体暂时 COOLED 时安全快进而不误报 NO_ELIGIBLE_SEEDS。
- Energy 分量范围、确定性和边界。
- RuleBased expected novelty 不进入能量。
- 风险深度提升增加价值。
- 高频 profile 降低 rarity。
- 无增益触发衰减/冷却。
- 基础设施失败不计无增益。
- 最大深度退休。
- Corpus 六种理由。
- 纯文字差异不进入 Corpus。
- WorkItem 合法/非法跳转，PENDING Seed 不可选择，dispatch sequence 仅在 Lease 时连续分配。
- Lease token、过期回收。
- retry 退避和上限。
- 系统性错误断路器。
- Budget 不超发。
- 指标空样本/P50/P95。
- Soak Probe 隔离且不会调用 CoverageStore、CorpusPolicy 或 SeedPool 晋升。
- 空批次、already-generated 批次和全重复候选会推进 iteration，并最终冷却/停滞而不是忙循环。
- deterministic rounds 与 throughput 的反馈滞后约束。

### 23.2 集成测试

使用确定性内存 Executor：

```text
2 initial seeds
  -> bootstrap
  -> select + energy + mutate
  -> bounded queue
  -> 2 workers out-of-order finish
  -> single writer coverage/corpus
  -> promote seed
  -> next round
```

验证：

- 上一轮 Coverage 影响下一轮。
- 高价值子种子成为父种子。
- Queue 不越界。
- Worker 可以乱序完成，但 Coverage/Corpus 必须按 dispatch sequence 提交。
- Candidate 不重复创建逻辑任务；Initial Case 和每次 Soak Probe 使用合法且不同的 source identity。
- 崩溃在 trajectory 后/coverage 前可恢复。
- 崩溃在 coverage 后/fuzzer commit 前可恢复。
- Pause 不再生成和 Lease。
- Resume 不重复 Corpus。
- transient retry，case failure 不盲目 retry。
- systemic failure 自动暂停。
- SQLite 重开一致。
- CLI 全流程可用。

### 23.3 Docker 多轮 E2E

```text
创建 Campaign
  -> bootstrap 至少 2 Seed
  -> concurrency=2
  -> 至少 3 mutation iterations
  -> 至少 6 accepted candidates
  -> 独立容器执行
  -> CoverageResult 可查询
  -> 至少 1 Corpus Entry
  -> 至少 1 Seed 晋升并再次被选择
  -> Pause 并确认无 Campaign 容器
  -> Resume 后再提交至少 1 WorkItem
  -> Stop 并确认无容器/volume
```

不硬编码“覆盖一定高于父种子”，闭环状态和价值判定分别测试。

### 23.4 Docker 恢复 E2E

故障点：

1. Lease 后、容器前。
2. 容器运行期间终止 Orchestrator。
3. trajectory commit 后、Coverage 前。
4. Coverage 后、Fuzzer commit 前。
5. pause grace timeout。
6. destroy 首次失败、恢复清理成功。

验证 Lease 回收、完成轨迹不盲目重跑、Coverage/Corpus/Seed 统计幂等、孤儿清理且不影响其他 Campaign。

### 23.5 回归

- 第一至第四周非 Docker 测试全部通过。
- Mutation path signature 回归继续通过。
- run/record/replay/fork/coverage/mutate CLI 不变。
- 不重复增加已证明确定性的 strict replay 次数测试。
- 不增加当前没有真实状态副作用的 stub state-delta 测试。

---

## 24. 验收标准

### 24.1 闭环

1. Initial Seed 完成 Bootstrap。
2. Seed 获得可解释 EnergyDecision。
3. Energy 限制候选数量。
4. accepted candidate 进入持久 Queue。
5. 多 Worker 独立容器执行。
6. Coverage 影响下一轮反馈和选种。
7. 高价值候选进入 Corpus。
8. 符合条件的候选晋升并再次被选择。
9. Budget/Stagnation 正常结束 Campaign。

### 24.2 并发与恢复

1. 内存 Queue 有界。
2. SQLite 业务提交单写者。
3. Lease 可续约、过期、回收。
4. Worker 可以乱序完成，结果按 dispatch sequence 缓冲和提交且不重复。
5. 重启可恢复。
6. PAUSED 时无活动 Worker/容器。
7. Resume 拒绝 digest 漂移。
8. 单任务错误不退出 Campaign。
9. 系统错误安全暂停。
10. 清理不误删其他 Campaign。

### 24.3 可追溯性

```text
Campaign Manifest
  -> EnergyDecision
  -> Parent Seed
  -> Mutation Batch/Candidate/Operator
  -> WorkItem/Attempt/Execution
  -> Trajectory/Replay
  -> CoverageResult/Risk Evidence
  -> Corpus Reasons
```

引用 digest 不匹配必须显式失败。

### 24.4 本机完成标准

- 单元、集成、Docker 多轮和恢复 E2E 通过。
- 本机基础设施 Soak 达到约定时长。
- 无不可恢复崩溃。
- 无遗留 Campaign 容器和 volume。
- 可暂停、退出、恢复和导出。
- Fake/RuleBased 不被描述为真实语义质量。

### 24.5 Server-gated 标准

- 真实模型和镜像 digest 锁定。
- 受控模型网络在 Linux 验收。
- 1 至 2 个 Target Profile smoke 通过。
- 24 小时有效 Fuzzing。
- 覆盖和停滞原因可查询。
- GPU/CPU/内存/模型队列无不可接受泄漏或饥饿。
- Mutator 有效率、重复率和吞吐有记录。
- 恢复演练成功。

服务器条件未满足不阻塞代码合并，但 M5“真实模型持续探索”保持待验收。

---

## 25. 风险与策略

| 风险 | 影响 | 处理 |
|---|---|---|
| 多 Store 无跨库事务 | 半提交 | 固定顺序、幂等 API、状态机和 Recovery |
| 随机 execution ID | 无法判断已执行 | 按 work/attempt 派生 ID |
| SQLite 并发写 | locked/线程错误 | Coordinator 单写者 |
| 无限生成 | 磁盘/内存增长 | bounded queue、max pending、按容量生成 |
| 高能量 Seed 饥饿其他 Seed | 探索坍缩 | Weighted Virtual Runtime |
| RuleBased 空间耗尽 | 本机提前停滞 | 记录测试替身局部 STAGNATED，不冒充场景完成；Soak Probe 单独统计 |
| Probe 污染覆盖 | 虚假增长 | measurement class 隔离 |
| 取消中断 cleanup | 遗留容器 | shield、cleanup timeout、恢复对账 |
| 全局 orphan cleanup | 误删其他 Campaign | Campaign label + active allowlist |
| retry 风暴 | 服务雪崩 | 有限 retry、退避、断路器 |
| 基础设施错误惩罚 Seed | 错误淘汰 | 只用正常 Coverage 更新 stagnation |
| Corpus/SeedPool 混淆 | 种子爆炸 | 独立模型与晋升 |
| 时间进入公式 | 恢复漂移 | logical iteration + virtual runtime |
| 模型配置漂移 | 不可比较 | Manifest 锁 digest |
| Agent/Mutator 抢 GPU | OOM/延迟 | server-gated 容量与独立配额 |
| 24h 只有重复输入 | 探索无意义 | Soak 与语义 Campaign 分开 |
| 风险不可达 | 浪费预算 | CampaignRiskScope 和停滞报告 |
| Prompt 日志泄漏 | 内容扩散 | 默认摘要，显式导出 |

---

## 26. 建议实施顺序

1. Fuzzer models、异常和 Canonical ID。
2. Config、Manifest 和版本锁定。
3. FuzzerStore Schema、事务和状态转移。
4. SeedPool 和 Weighted Virtual Runtime。
5. EnergyScheduler 和 Decision 审计。
6. CorpusPolicy 和 Outcome Index。
7. WorkQueue、Lease、retry、dead letter。
8. CandidateExecutor。
9. run_test_case 的 execution ID/run context 兼容扩展。
10. Docker Campaign label 和精确 orphan cleanup。
11. RecoveryManager。
12. 单 Worker 闭环。
13. bounded queue 和多 Worker。
14. pause/resume/stop。
15. Metrics、Snapshot 和 export。
16. Campaign CLI。
17. 单元和集成故障注入。
18. Docker 多轮和恢复 E2E。
19. 本机 Soak。
20. 服务器到位后的 Profile、网络、模型和 24 小时验收。

以上仅表示实现依赖关系。每一步必须保持前四周回归通过。

---

## 27. 第五周完成定义

```text
Campaign 创建并锁定版本 digest
  -> initial TestCase bootstrap
  -> Coverage 基线
  -> SeedPool ACTIVE
  -> EnergyDecision
  -> 最新 Coverage Feedback 驱动 Mutator
  -> Candidate 进入持久 WorkItem 与有界 Queue
  -> 多 Worker 隔离执行 Prompt/Fork
  -> 轨迹提交并销毁容器
  -> Coordinator 幂等提交 Coverage
  -> CorpusPolicy 保存高价值证据
  -> 候选晋升新 Seed
  -> 新 Seed 进入下一轮
  -> 高频/无增益 Seed 衰减或冷却
  -> Campaign 可暂停、退出、恢复
  -> Budget/停滞/用户请求正常结束
  -> 状态、血缘、错误和指标可查询
```

第五周交付的是可持续、可恢复、可审计的覆盖率引导灰盒 Fuzzer。没有服务器时完成确定性闭环和基础设施耐久性；服务器到位后完成真实本地模型、受控网络、Target Profile 和 24 小时有效探索的部署验收。
